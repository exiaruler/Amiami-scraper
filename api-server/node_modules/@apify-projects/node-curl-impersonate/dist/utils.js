"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.escapeUrl = escapeUrl;
/**
 * Escapes and cleans the URL. The URL is escaped and normalized to a valid URL.
 * - We especially need to escape single quotes (`'`), as they are used in the curl command.
 * - Quotes are escaped to their ASCII code (`\x27`), which is later decoded by the shell.
 */
function escapeUrl(url) {
    const normalizedUrl = new URL(url).toString();
    return normalizedUrl.replace(/'/g, '\x27');
}
//# sourceMappingURL=utils.js.map